{
  "name": "@hls-app/backend",
  "root": "apps/backend",
  "sourceRoot": "apps/backend/src",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvnw spring-boot:run -Dspring-boot.run.jvmArguments=\"-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5555\"",
        "cwd": "apps/backend",
        "envFile": "apps/backend/.env"
      }
    },
    "build": {
      "command": "mvnw package",
      "cwd": "apps/backend",
      "outputs": [
        "apps/backend/target"
      ]
    }
  }
}